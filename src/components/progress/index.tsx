import { Spinner, SpinnerContainer } from './spinner';

const ProgressComponent = () => {
	return (
		<SpinnerContainer>
			<Spinner />
		</SpinnerContainer>
	);
};

export default ProgressComponent;
