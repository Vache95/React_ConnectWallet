import { flex } from 'components/ui/flex';

import styled from 'styled-components';

export const ConnectM = styled.div`
	${flex('center', false)}
`;

export const ConnectMText = styled.p`
	font-family: Lato;
	font-weight: 700;
	font-size: 16px;
	line-height: 24px;
	color: #212225;
	margin-left: 13px;
`;
